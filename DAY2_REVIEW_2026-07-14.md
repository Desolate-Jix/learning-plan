# Day 2｜2026-07-14｜StringBuilder 复刷与 ArrayList 线性扫描

## 今日总结果

今天完成了两项内容：

1. 闭卷重写 `Merge Strings Alternately` 的 `StringBuilder` 版本并 Accepted；
2. 独立设计两遍扫描方案，完成 [Kids With the Greatest Number of Candies](https://leetcode.com/problems/kids-with-the-greatest-number-of-candies/) 并 Accepted。

- **LeetCode 75 新题进度：** 2 / 75
- **今日新题掌握等级：** B——算法步骤由自己提出，`List<Boolean>` / `ArrayList` 语法接受提示

---

# 第一部分：Merge Strings Alternately 优化重写

## 1. 结果

- 题目：[Merge Strings Alternately](https://leetcode.com/problems/merge-strings-alternately/)
- 结果：优化重写版本 Accepted
- 掌握等级：B（整体结构独立写出，查过少量 Java 语法并接受提示）
- 这次不计为新的 LeetCode 题数，属于 Day 1 的高质量复刷。

## 2. 自己最终通过的代码

```java
class Solution {
    public String mergeAlternately(String word1, String word2) {
        StringBuilder str = new StringBuilder();
        int l1 = word1.length();
        int l2 = word2.length();
        int i = 0;

        for (i = 0; i < l1 && i < l2; i++) {
            str.append(word1.charAt(i));
            str.append(word2.charAt(i));
        }

        if (l1 > l2) {
            str.append(word1.substring(i, l1));
        }

        if (l1 < l2) {
            str.append(word2.substring(i, l2));
        }

        return str.toString();
    }
}
```

## 3. 出现的语法与理解问题

| 写法或问题 | 原因 | 正确理解 |
|---|---|---|
| `New StringBuilder` | Java 关键字区分大小写 | 使用小写 `new` |
| `new StringBuilder` | 创建对象时漏掉构造方法括号 | `new StringBuilder()` |
| `reached end of file while parsing` | `{` 与 `}` 数量没有配对 | 方法和类都要有对应的结束 `}` |
| 在 `for (int i = ...)` 后还想使用 `i` | `i` 只在该 `for` 代码块范围内存在 | 需要在循环前声明 `int i = 0;` |
| `substring(i, l1 - 1)` | 误以为结束下标会被包含 | `substring` 的结束位置不包含，应传 `l1` |
| “最后一位取不取？” | 容易把下标和长度混淆 | `substring(start, end)` 取 `[start, end)` |

## 4. `substring` 核心规则

```java
String word = "abcdef";
word.substring(1, 4);
```

结果是：

```text
bcd
```

包含下标 `1、2、3`，不包含下标 `4`。

```java
word.substring(2, 4); // "cd"
word.substring(2);    // "cdef"
```

## 5. Reviewer 点评

### 做得好的地方

- 把两个同步下标简化成一个 `i`；
- 使用 `StringBuilder.append()`，避免循环中反复创建字符串；
- 把字符串长度保存为 `l1`、`l2`；
- 知道主循环结束时，`i` 正好指向剩余部分开始位置；
- 能自行修正 `substring` 边界并通过测试。

### 可以改进

1. `str` 可以改名为 `result`；
2. `i` 已在循环外初始化，使用 `while` 会更自然，但当前 `for` 也正确；
3. 两个 `if` 可以去掉，因为没有剩余字符的一方调用 `substring(i)` 会得到空字符串；
4. 可以提前给 `StringBuilder` 分配最终容量。

## 6. 更简洁的写法

```java
class Solution {
    public String mergeAlternately(String word1, String word2) {
        int i = 0;
        int length1 = word1.length();
        int length2 = word2.length();

        StringBuilder result = new StringBuilder(length1 + length2);

        while (i < length1 && i < length2) {
            result.append(word1.charAt(i));
            result.append(word2.charAt(i));
            i++;
        }

        result.append(word1.substring(i));
        result.append(word2.substring(i));

        return result.toString();
    }
}
```

- 时间复杂度：`O(n + m)`
- 结果空间：`O(n + m)`

---

# 第二部分：Kids With the Greatest Number of Candies

## 1. 题目结果

- 题目：[1431. Kids With the Greatest Number of Candies](https://leetcode.com/problems/kids-with-the-greatest-number-of-candies/)
- 结果：Accepted
- 掌握等级：B
- 核心算法由自己提出：
  1. 第一遍扫描数组，找到当前最大糖果数；
  2. 第二遍扫描，判断每个孩子加上额外糖果后是否达到或超过最大值；
  3. 将每个判断结果依次加入 `List<Boolean>`。

## 2. 自己最终通过的代码

```java
class Solution {
    public List<Boolean> kidsWithCandies(int[] candies, int extraCandies) {
        int i = candies.length;
        int maxchild = 0;

        for (int a = 0; a < i; a++) {
            if (candies[a] > maxchild) {
                maxchild = candies[a];
            }
        }

        List<Boolean> candy = new ArrayList<>();

        for (int b = 0; b < i; b++) {
            if (candies[b] + extraCandies >= maxchild) {
                candy.add(true);
            } else {
                candy.add(false);
            }
        }

        return candy;
    }
}
```

> 上面只统一了缩进和空格，保留实际 Accepted 的逻辑。

## 3. 今日 Java 知识：数组与 List

### 数组

```java
int[] nums = new int[5];
nums[0] = 10;
int value = nums[0];
int length = nums.length;
```

- 长度创建后固定；
- 使用 `[]` 读取和修改；
- 长度使用 `.length`。

### List / ArrayList

```java
List<Boolean> result = new ArrayList<>();
result.add(true);
Boolean value = result.get(0);
result.set(0, false);
int size = result.size();
```

- 可以不断 `add()` 新元素；
- 使用 `get()`、`set()`，不能使用数组的 `[]`；
- 元素数量使用 `size()`；
- `List` 是接口，`ArrayList` 是常用实现。

### 为什么写 `Boolean`，不能写 `boolean`

Java 泛型参数必须是引用类型：

```java
List<Boolean> values; // 正确
List<boolean> values; // 错误

List<Integer> nums;   // 正确
List<int> nums;       // 错误
```

Java 会在 `boolean` 和 `Boolean` 之间自动装箱、拆箱，所以可以直接：

```java
result.add(true);
```

## 4. 本题出现的语法错误

| 当时的写法 / 问题 | 原因 | 正确写法 |
|---|---|---|
| `boolean candy[];` | 只声明了数组，没有分配长度；而且方法要求返回 `List<Boolean>` | `List<Boolean> candy = new ArrayList<>();` |
| `return candy[];` | `[]` 不能写在返回表达式后 | 数组返回 `return candy;`；本题返回 List 同样是 `return candy;` |
| `if (candies[a] maxchild)` | 两个值之间缺少比较运算符 | `if (candies[a] > maxchild)` |
| `list<boolean>` | Java 类名大小写错误；泛型不能使用基本类型 | `List<Boolean>` |
| `Arraylist` | 类名中 `L` 必须大写 | `ArrayList` |
| `new ArrayList<>` | 创建对象需要调用构造方法 | `new ArrayList<>()` |
| `candy[b] = true` | 把 List 当成数组使用；新 List 里还没有第 `b` 个位置 | `candy.add(true)` |

## 5. 两遍线性扫描

这道题使用两次循环：

```text
第一遍 O(n)：找最大值
第二遍 O(n)：生成答案
```

因此：

```text
O(n) + O(n) = O(2n) = O(n)
```

大 O 记号忽略常数系数，最终时间复杂度是 `O(n)`。

- 时间复杂度：`O(n)`
- 输出空间：`O(n)`
- 除返回结果外的额外空间：`O(1)`

## 6. Reviewer 点评

### 做得好的地方

1. 没有为每个孩子重新寻找最大值，而是先找一次最大值；
2. 正确使用了两遍线性扫描，整体仍是 `O(n)`；
3. 正确处理了“并列最大”，使用 `>=` 而不是只使用 `>`；
4. 在理解 `List` 后，能正确用 `add(true)` 和 `add(false)` 生成结果；
5. 算法拆分清楚：先准备比较基准，再生成答案。

### 可以改进的地方

#### 变量名更明确

```java
int i = candies.length;
int maxchild = 0;
List<Boolean> candy = new ArrayList<>();
```

可以改成：

```java
int length = candies.length;
int maxCandies = 0;
List<Boolean> result = new ArrayList<>(length);
```

#### 最大值初始化更稳健

本题约束保证糖果数至少为 1，所以 `maxchild = 0` 正确。

更通用的写法是：

```java
int maxCandies = candies[0];
```

这样即使数组中允许负数，也能正确找最大值；前提是数组非空。

#### `if / else` 可以直接简化成布尔表达式

当前：

```java
if (candies[b] + extraCandies >= maxchild) {
    candy.add(true);
} else {
    candy.add(false);
}
```

因为比较表达式本身已经产生 `true` 或 `false`，可以直接：

```java
result.add(candies[b] + extraCandies >= maxCandies);
```

## 7. 推荐改进版本

```java
class Solution {
    public List<Boolean> kidsWithCandies(int[] candies, int extraCandies) {
        int maxCandies = candies[0];

        for (int candy : candies) {
            maxCandies = Math.max(maxCandies, candy);
        }

        List<Boolean> result = new ArrayList<>(candies.length);

        for (int candy : candies) {
            result.add(candy + extraCandies >= maxCandies);
        }

        return result;
    }
}
```

这个版本新增了两项可后续学习的写法：

- 增强 `for` 循环：`for (int candy : candies)`；
- `Math.max(a, b)`。

当前不要求立即背下来。先确认能看懂，再在后续题目中重复使用。

## 8. 闭卷复习题

1. 数组为什么使用 `nums.length`，List 为什么使用 `list.size()`？
2. `List<Boolean>` 为什么不能写成 `List<boolean>`？
3. 新建空的 `ArrayList` 后，为什么不能直接写 `result[0] = true`？
4. `add()`、`get()`、`set()` 分别做什么？
5. 为什么两个 `O(n)` 循环加起来仍是 `O(n)`？
6. 为什么本题判断条件需要使用 `>=`？
7. 为什么可以把整个 `if / else` 简化成：

```java
result.add(candy + extraCandies >= maxCandies);
```

## 9. 后续复习

- [x] 独立提出两遍扫描算法并 Accepted
- [x] 学会创建 `List<Boolean>` 和 `ArrayList<>()`
- [x] 学会通过 `add()` 向 List 追加元素
- [ ] 三天内不看答案重写本题
- [ ] 能独立写出 `result.add(比较表达式)` 的简化版本
- [ ] 能解释 `O(n) + O(n)` 为什么仍然是 `O(n)`
