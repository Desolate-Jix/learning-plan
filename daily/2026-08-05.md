# 2026-08-05｜Day 19：二分查找与字符串频率

> 今天是周三，新西兰时间。昨天完成 1679 后因晚上开会停止；今天优先处理顺延的 374，再完成一题 Medium。最多两题，不补昨天缺少的额外题位。

## 今日状态

- **开始进度：** 20 / 75
- **当前进度：** 21 / 75
- **374. Guess Number Higher or Lower：** Accepted，25 / 25
- **1657. Determine if Two Strings Are Close：** 待完成

---

# 任务 A｜374. Guess Number Higher or Lower

[打开题目](https://leetcode.com/problems/guess-number-higher-or-lower/)

## 完成结果

- **难度：** Easy
- **结果：** Accepted，25 / 25
- **提交记录：** 0 ms、41.90 MB；单次排名仅作参考
- **主题：** 二分查找、题目提供的 API、整数溢出
- **时间复杂度：** `O(log n)`
- **额外空间：** `O(1)`
- **掌握等级：** B

### 为什么记为 B

这次能够根据 `guess(current)` 的返回结果写出左右边界收缩，但实现过程中经过了多轮提示和调试：

1. 理解 `guess(int num)` 是 LeetCode 提供的方法/API，不需要自己实现；
2. `guess(current)` 返回 `int`，不能写成 `!guess(current) == 0`，应比较 `guess(current) != 0`；
3. Java 要求所有执行路径返回 `int`，因此循环后需要兜底 `return -1`；
4. `guess(current) == 1` 表示当前值太小，应移动 `left`；返回 `-1` 表示当前值太大，应移动 `right`；
5. 边界必须使用 `current + 1` 和 `current - 1`，否则中点可能不变并形成死循环；
6. 最初使用 `(left + right) / 2`，在大整数测试上溢出并出现 TLE；改为防溢出写法后通过。

核心算法不是完全独立完成，因此暂记 B。后续闭卷重写并能主动写出防溢出中点公式后再提升等级。

## 最终 Accepted 代码

```java
public class Solution extends GuessGame {
    public int guessNumber(int n) {
        int left = 1;
        int right = n;

        while (left <= right) {
            int current = left + (right - left) / 2;
            int result = guess(current);

            if (result == 1) {
                left = current + 1;
            } else if (result == -1) {
                right = current - 1;
            } else {
                return current;
            }
        }

        return -1;
    }
}
```

## `guess()` 返回值与边界方向

```text
guess(current) == 0
→ current 就是答案

guess(current) == 1
→ current 小于隐藏数字
→ 答案在右侧
→ left = current + 1

guess(current) == -1
→ current 大于隐藏数字
→ 答案在左侧
→ right = current - 1
```

## 调试记录

### 1. `!` 不能用于整数

错误：

```java
!guess(current) == 0
```

`guess(current)` 返回的是 `int`，而 `!` 只能对 `boolean` 使用。

### 2. 边界方向与 `±1`

错误方向会把答案所在区间丢掉；不加 `1` 或减 `1` 时，`current` 可能在下一轮保持不变。

### 3. 中点整数溢出

首次可运行版本使用：

```java
int current = (left + right) / 2;
```

在以下大输入上出现：

```text
Time Limit Exceeded
14 / 25 testcases passed
n = 2126753390
pick = 1702766719
```

原因是 `left + right` 可能超过：

```text
Integer.MAX_VALUE = 2147483647
```

溢出后中点可能变成负数，区间不能正确收缩。

最终使用：

```java
int current = left + (right - left) / 2;
```

`right - left` 在合法区间内不会超过 `int` 上限，从而避免加法溢出。

## 复习任务

- [ ] 闭卷重写标准二分查找；
- [ ] 能解释为什么边界更新必须使用 `mid ± 1`；
- [ ] 能解释 `(left + right) / 2` 的溢出风险；
- [ ] 主动写出 `left + (right - left) / 2`；
- [ ] 能根据 API 返回值正确判断移动左边界还是右边界。

---

# 任务 B｜1657. Determine if Two Strings Are Close

[打开题目](https://leetcode.com/problems/determine-if-two-strings-are-close/)

- **难度：** Medium
- **状态：** 待完成
- **主题：** 字符频率、集合条件、排序或计数
- **目标复杂度：** `O(n)` 或 `O(n log n)` 时间、`O(1)` 额外空间（字符集固定）

## 今日停止规则

- 两题完成后停止，不追加第三题；
- 1657 独立思考 35～45 分钟，超时后按“小提示 → 思路提示 → 完整题解”推进；
- 如果第二题消耗过多，只记录题意、手算、模式和骨架，不熬夜补。
