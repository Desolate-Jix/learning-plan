# Day 2｜Can Place Flowers 第一次尝试

## 当前状态

- 题目：[605. Can Place Flowers](https://leetcode.com/problems/can-place-flowers/)
- 页面结果：本地样例 Case 1、Case 2 通过
- 当前结论：**尚不能计为题目完成**。截图显示的是 Testcase 样例通过，不是提交全部隐藏测试后的 Accepted。

## 第一次思路

当前代码先统计已有花朵数量 `counter`，再根据数组长度计算：

```java
int nmax = l / 2 + 1;
int nmin = l / 2;
```

然后根据第一个位置是不是 `1`，判断：

```java
counter + n <= nmax
```

或：

```java
counter + n <= nmin
```

这个思路把问题简化成：

> 数组长度最多能容纳多少朵花，减去已经存在的花，是否还够放 `n` 朵。

## 做得好的地方

1. 想到了整数除法：`l / 2` 会自动舍弃小数部分；
2. 尝试从“数组最大容量”角度寻找数学规律；
3. 代码可以编译，并通过题目给出的两个样例；
4. 能先统计已有花朵数量，说明正在主动把问题拆成步骤。

## 核心逻辑问题

这道题不能只看：

- 数组总长度；
- 已有花朵总数；
- 第一个位置是否为 `1`。

因为**已有花朵分布在哪些位置**会直接决定剩余空位能否种花。

下面两个花坛可能长度相同、已有花朵数量相同，但能够新增的花朵数不同。

### 反例

```text
flowerbed = [1, 0, 0, 1, 0, 0, 0]
n = 2
```

当前代码计算：

```text
长度 l = 7
nmax = 7 / 2 + 1 = 4
已有花朵 counter = 2
counter + n = 4
```

因为第一个位置是 `1`，代码会判断 `4 <= 4`，返回 `true`。

但实际只能在下标 `5` 再种一朵：

```text
[1, 0, 0, 1, 0, 1, 0]
```

不能再种第二朵，因此正确答案是 `false`。

## 今天新增的关键认识

> 通过示例不代表通过整道题。

LeetCode 编辑器中的：

```text
Testcase -> Accepted
```

通常只表示当前运行的样例通过。

只有点击 **Submit**，并通过全部隐藏测试，才能把题目记录为正式 Accepted。

## 下一步思考方向

不要计算理论最大容量。改为从左到右检查每个位置：

1. 当前格子必须是 `0`；
2. 左边没有花；
3. 右边没有花；
4. 满足时就在当前位置种一朵，并更新数组或计数；
5. 种够 `n` 朵后立即返回 `true`。

边界位置需要特殊理解：

- 下标 `0` 没有左邻居，可以把“数组外面”视为没有花；
- 最后一个下标没有右邻居，同样视为没有花。

暂不直接记录完整答案，先根据这个方向修改代码。

## 当前掌握评级

- 算法状态：进行中
- 当前评级：未评估
- 主要卡点：用总数量推断可种位置，忽略了花朵的具体分布和相邻约束
